/*
|--------------------------------------------------------------------------
| Preloaded File
|--------------------------------------------------------------------------
|
| Any code written inside this file will be executed during the application
| boot.
|
*/
// import Redis from '@ioc:Adonis/Addons/Redis'
// import runtimeManager from '@ioc:JsRuntime/RuntimeManager'

// Redis.psubscribe('mqttHook:*', (channel:string,message:string) => {
//   const messageObj = JSON.parse(message)
//   //console.log(message)
//   runtimeManager.exec(messageObj.projectId, messageObj.readToken, messageObj.writeToken,messageObj.macroName, messageObj.macroId,messageObj.script,messageObj.payload)
// })
